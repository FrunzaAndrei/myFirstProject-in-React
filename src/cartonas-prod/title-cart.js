import React from "react";

const TitleCart = props => {
  return (
    <div className="Title-cartonas">
      <p className="p-title-cart"> {props.title} </p>
    </div>
  );
};

export default TitleCart;